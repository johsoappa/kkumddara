import type { Config } from "tailwindcss";

// ====================================================
// 꿈따라 Tailwind 설정
// 커스텀 컬러를 추가해서 className에서 바로 사용 가능
// 예: className="bg-brand-red text-white"
// ====================================================

const config: Config = {
  // [확인 포인트] content 경로가 맞지 않으면 스타일이 적용 안 됩니다
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        // ----------------------------------------
        // 시맨틱 Primary — 신규 코드에서 이 토큰 사용
        // ----------------------------------------
        primary: {
          DEFAULT: "#E84B2E",    // 기본 (= brand-red)
          hover:   "#C83A20",    // hover / active (WCAG 4.6:1 on white ✅)
          soft:    "#FFF0EB",    // 배경용 (= brand-light)
          text:    "#C83A20",    // 소형 텍스트 on white / on soft
        },
        // ----------------------------------------
        // 火 (불) - 포인트 컬러: 에너지·열정
        // ----------------------------------------
        brand: {
          red: "#E84B2E",        // 메인 포인트 (꿈따라 레드오렌지)
          orange: "#FF7043",     // 서브 포인트 (딥오렌지)
          soft: "#FFA270",       // 밝은 강조 (소프트 오렌지)
          light: "#FFF0EB",      // 포인트 배경 (연한 오렌지)
        },
        // ----------------------------------------
        // 金 (금) - 베이스 컬러: 신뢰·안정
        // ----------------------------------------
        base: {
          white: "#FFFFFF",      // 메인 배경
          off: "#F8F8F8",        // 서브 배경
          card: "#F2F2F2",       // 카드 배경
          border: "#E0E0E0",     // 구분선/보더
          muted: "#9E9E9E",      // 보조 텍스트
          text: "#212121",       // 메인 텍스트 (차콜 블랙)
        },
        // ----------------------------------------
        // 상태 컬러
        // ----------------------------------------
        status: {
          success: "#4CAF50",    // 성공/완료 (그린)
          warning: "#FF9800",    // 경고 (오렌지)
          disabled: "#BDBDBD",   // 비활성 (그레이)
        },
      },
      // ----------------------------------------
      // 폰트 설정
      // ----------------------------------------
      fontFamily: {
        sans: ["Pretendard", "Apple SD Gothic Neo", "sans-serif"],
      },
      // ----------------------------------------
      // 모바일 최대 너비
      // ----------------------------------------
      maxWidth: {
        mobile: "430px",
      },
      // ----------------------------------------
      // 커스텀 border-radius
      // ----------------------------------------
      borderRadius: {
        card: "12px",
        "card-lg": "16px",
        button: "12px",
      },
      // ----------------------------------------
      // 커스텀 그림자 (카드용)
      // ----------------------------------------
      boxShadow: {
        card: "0 2px 12px rgba(0, 0, 0, 0.08)",
        "card-hover": "0 4px 20px rgba(0, 0, 0, 0.12)",
        nav: "0 -1px 8px rgba(0, 0, 0, 0.08)",
      },
    },
  },
  plugins: [],
};

export default config;
